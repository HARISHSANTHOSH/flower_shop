[phases.build]

cmds = ["pip install -r requirements.txt"]

[start]

cmd = "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn flowerproject.wsgi:application --bind 0.0.0.0:$PORT"