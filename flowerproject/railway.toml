[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn flowerproject.wsgi:application --bind 0.0.0.0:$PORT & celery -A flowerproject worker --loglevel=info & wait"